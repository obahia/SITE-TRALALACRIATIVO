TASK 6: Corrigir Página /sucesso com Verificação de Pagamento
QA SCENARIOS & VERIFICATION

=== SCENARIO 1: Valid Order with Status 'pago' ===
SETUP:
- User logged in with ID: user-123
- Order exists in DB with:
  - id: order-abc123
  - status: 'pago'
  - user_id: user-123

FLOW:
1. Navigate to /sucesso?orderId=order-abc123
2. Page loads, useEffect triggers checkOrderStatus()
3. Query: supabase.from('orders').select('status, user_id').eq('id', 'order-abc123').single()
4. Result: { status: 'pago', user_id: 'user-123' }
5. Verify order.user_id === user.id ✓
6. setOrderStatus('pago')
7. clearCart() called
8. setLoading(false)

EXPECTED OUTPUT:
- ✅ Green CheckCircle icon displayed
- ✅ "Pagamento Confirmado!" heading shown
- ✅ Order ID displayed: "#order-ab"
- ✅ "Continuar a Comprar" and "Voltar ao Início" buttons visible
- ✅ clearCart() executed (cart emptied in Supabase)

=== SCENARIO 2: Valid Order with Status 'pendente' (Polling) ===
SETUP:
- User logged in with ID: user-456
- Order exists in DB with:
  - id: order-def456
  - status: 'pendente'
  - user_id: user-456

FLOW:
1. Navigate to /sucesso?orderId=order-def456
2. Page loads, useEffect triggers checkOrderStatus()
3. Query returns: { status: 'pendente', user_id: 'user-456' }
4. setOrderStatus('pendente')
5. setLoading(false) NOT called (still loading)
6. pollingInterval set to 3s
7. timeoutId set to 30s

POLLING BEHAVIOR (3s interval):
- T=0s: Initial check → status: 'pendente'
- T=3s: Re-query → status: 'pendente'
- T=6s: Re-query → status: 'pendente'
- ...
- T=30s: Timeout triggered
  - clearInterval(pollingInterval)
  - setPollingTimeout(true)
  - setLoading(false)

EXPECTED OUTPUT (during polling):
- ✅ Blue Loader icon spinning
- ✅ "A Verificar Pagamento" heading shown
- ✅ "Estamos a confirmar o seu pagamento. Por favor, aguarde..." message
- ✅ No buttons visible (loading state)

EXPECTED OUTPUT (after 30s timeout):
- ✅ Yellow ShoppingBag icon displayed
- ✅ "A Verificar Pagamento" heading shown
- ✅ "O seu pedido #order-de está a ser processado..." message
- ✅ "Continuar a Comprar" and "Voltar ao Início" buttons visible

=== SCENARIO 3: Order Status Changes from 'pendente' to 'pago' (Webhook) ===
SETUP:
- User logged in with ID: user-789
- Order initially: { id: order-ghi789, status: 'pendente', user_id: user-789 }
- Webhook updates status to 'pago' at T=9s

FLOW:
1. Navigate to /sucesso?orderId=order-ghi789
2. T=0s: Initial check → status: 'pendente'
3. T=3s: Re-query → status: 'pendente'
4. T=6s: Re-query → status: 'pendente'
5. T=9s: Webhook updates DB → status: 'pago'
6. T=9s: Re-query → status: 'pago'
7. clearInterval(pollingInterval)
8. clearTimeout(timeoutId)
9. clearCart() called
10. setLoading(false)

EXPECTED OUTPUT:
- ✅ Loader visible for ~9 seconds
- ✅ Green CheckCircle icon displayed
- ✅ "Pagamento Confirmado!" heading shown
- ✅ Order ID displayed: "#order-gh"
- ✅ clearCart() executed

=== SCENARIO 4: Invalid orderId (Missing or Malformed) ===
SETUP:
- User logged in

FLOW:
1. Navigate to /sucesso (no orderId parameter)
2. useEffect checks: if (!orderId || !user) { navigate('/'); }
3. Redirect to home page

EXPECTED OUTPUT:
- ✅ Immediate redirect to /
- ✅ No error messages
- ✅ No API calls made

=== SCENARIO 5: Order Not Found in Database ===
SETUP:
- User logged in with ID: user-999
- orderId: order-notfound

FLOW:
1. Navigate to /sucesso?orderId=order-notfound
2. Query: supabase.from('orders').select(...).eq('id', 'order-notfound').single()
3. Result: error (order not found)
4. if (error || !order) { navigate('/'); }
5. Redirect to home page

EXPECTED OUTPUT:
- ✅ Redirect to /
- ✅ No error messages displayed
- ✅ No clearCart() called

=== SCENARIO 6: Order Belongs to Different User ===
SETUP:
- Current user: user-111
- Order in DB: { id: order-jkl111, status: 'pago', user_id: user-222 }

FLOW:
1. Navigate to /sucesso?orderId=order-jkl111
2. Query returns: { status: 'pago', user_id: 'user-222' }
3. Verify: order.user_id !== user.id
4. if (order.user_id !== user.id) { navigate('/'); }
5. Redirect to home page

EXPECTED OUTPUT:
- ✅ Redirect to /
- ✅ No success message displayed
- ✅ No clearCart() called
- ✅ Security: User cannot access other users' orders

=== SCENARIO 7: User Not Authenticated ===
SETUP:
- User not logged in (user === null)

FLOW:
1. Navigate to /sucesso?orderId=order-mno222
2. useEffect checks: if (!orderId || !user) { return; }
3. No API calls made
4. Page renders null (fallback)

EXPECTED OUTPUT:
- ✅ No error messages
- ✅ No API calls
- ✅ Page renders nothing (null)

=== VERIFICATION CHECKLIST ===
✅ Query logic: supabase.from('orders').select('status, user_id').eq('id', orderId).single()
✅ Polling interval: 3000ms (3 seconds)
✅ Polling timeout: 30000ms (30 seconds)
✅ clearCart() only called when status === 'pago'
✅ Redirect logic: Invalid orderId or order.user_id !== user.id
✅ Loading states: 3 distinct states (loading, timeout, success)
✅ Cleanup: pollingInterval and timeoutId cleared on unmount
✅ Design preserved: Same icons, layout, colors as original
✅ Imports: useAuth, useNavigate, supabase, Loader icon added
✅ Dependencies: [orderId, user, navigate, clearCart] in useEffect

=== CODE REVIEW ===
File: src/pages/sucesso.jsx
Lines: 1-180 (original: 48 lines)

Key additions:
- Line 5: import { useAuth } from '../context/AuthContext'
- Line 6: import { supabase } from '../services/supabase'
- Line 2: import { useNavigate } from 'react-router-dom'
- Line 3: Added Loader icon import
- Line 10: const navigate = useNavigate()
- Line 13: const { user } = useAuth()
- Line 32-61: checkOrderStatus() async function
- Line 68-76: Polling setup (3s interval, 30s timeout)
- Line 80-83: Cleanup on unmount
- Line 85-100: Loading state UI
- Line 103-130: Timeout state UI
- Line 133-165: Success state UI

All requirements met ✓
