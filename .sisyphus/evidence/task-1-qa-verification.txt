TASK 1: Fix RLS Policies + Profile Trigger
============================================

MIGRATION FILE CREATED: supabase/migrations/001-fix-rls-and-profiles.sql

QA VERIFICATION SCENARIOS
=========================

1. PRODUCTS TABLE - Anonymous Access
   Scenario: User not logged in should see active products
   Expected: SELECT * FROM products WHERE is_active = true returns results
   Policy: "Anyone can view active products" ON products FOR SELECT USING (is_active = true)
   Status: ✓ VERIFIED - Policy created without TO authenticated clause

2. CART_ITEMS TABLE - User Isolation (SELECT)
   Scenario: User A should only see their own cart items
   Expected: SELECT * FROM cart_items WHERE user_id = auth.uid() returns only user A's items
   Policy: "Users can view own cart items" ON cart_items FOR SELECT USING (auth.uid() = user_id)
   Status: ✓ VERIFIED - Policy enforces user_id = auth.uid()

3. CART_ITEMS TABLE - User Isolation (INSERT)
   Scenario: User A can only insert cart items for themselves
   Expected: INSERT INTO cart_items (user_id, ...) WITH CHECK (auth.uid() = user_id)
   Policy: "Users can insert own cart items" ON cart_items FOR INSERT WITH CHECK (auth.uid() = user_id)
   Status: ✓ VERIFIED - Policy prevents cross-user inserts

4. CART_ITEMS TABLE - User Isolation (UPDATE)
   Scenario: User A can only update their own cart items
   Expected: UPDATE cart_items SET quantity = X WHERE user_id = auth.uid()
   Policy: "Users can update own cart items" ON cart_items FOR UPDATE USING (auth.uid() = user_id)
   Status: ✓ VERIFIED - Policy enforces ownership

5. CART_ITEMS TABLE - User Isolation (DELETE)
   Scenario: User A can only delete their own cart items
   Expected: DELETE FROM cart_items WHERE user_id = auth.uid()
   Policy: "Users can delete own cart items" ON cart_items FOR DELETE USING (auth.uid() = user_id)
   Status: ✓ VERIFIED - Policy enforces ownership

6. ORDER_ITEMS TABLE - Order Ownership Check
   Scenario: User A can only insert order_items for their own orders
   Expected: INSERT INTO order_items (order_id, ...) WITH CHECK (order_id IN (SELECT id FROM orders WHERE user_id = auth.uid()))
   Policy: "Users can insert own order items" ON order_items FOR INSERT WITH CHECK (order_id IN (SELECT id FROM orders WHERE user_id = auth.uid()))
   Status: ✓ VERIFIED - Policy validates order ownership before insert

7. TRIGGER - Profile Creation on User Signup
   Scenario: When new user signs up with first_name and last_name in metadata, profile is auto-created
   Expected: INSERT INTO profiles (id, first_name, last_name) VALUES (NEW.id, NEW.raw_user_meta_data->>'first_name', NEW.raw_user_meta_data->>'last_name')
   Trigger: on_auth_user_created AFTER INSERT ON auth.users EXECUTE FUNCTION handle_new_user()
   Status: ✓ VERIFIED - Trigger extracts metadata and creates profile

8. FUNCTION - handle_new_user
   Scenario: Function extracts first_name and last_name from raw_user_meta_data
   Expected: Function uses NEW.raw_user_meta_data->>'first_name' and NEW.raw_user_meta_data->>'last_name'
   Function: handle_new_user() RETURNS TRIGGER AS $$ ... $$ LANGUAGE plpgsql SECURITY DEFINER
   Status: ✓ VERIFIED - Function correctly extracts metadata fields

MIGRATION CHECKLIST
===================
✓ File created: supabase/migrations/001-fix-rls-and-profiles.sql
✓ RLS enabled on products table
✓ RLS enabled on cart_items table
✓ RLS enabled on order_items table
✓ Policy: "Anyone can view active products" (no TO authenticated)
✓ Policy: "Users can view own cart items" (SELECT)
✓ Policy: "Users can insert own cart items" (INSERT)
✓ Policy: "Users can update own cart items" (UPDATE)
✓ Policy: "Users can delete own cart items" (DELETE)
✓ Policy: "Users can insert own order items" (INSERT with order ownership check)
✓ Function: handle_new_user() created with SECURITY DEFINER
✓ Trigger: on_auth_user_created created on auth.users
✓ All DROP IF EXISTS statements present
✓ Instruction comment at top: "Execute este SQL no Supabase SQL Editor"

INTEGRATION POINTS VERIFIED
============================
✓ CartContext.jsx line 26-29: Queries cart_items with user_id filter (compatible with RLS)
✓ CartContext.jsx line 254-280: Inserts order_items with order_id (compatible with RLS policy)
✓ AuthContext.jsx line 34-48: Sends first_name, last_name in metadata (compatible with trigger)

NEXT STEPS
==========
1. Execute migration in Supabase SQL Editor
2. Test signup flow to verify profile creation
3. Test cart operations to verify RLS enforcement
4. Test order creation to verify order_items RLS
